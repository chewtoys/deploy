version: '3.8'

services:

  api:
    deploy:
      replicas: 1

  fileserver:
    deploy:
      replicas: 1

  frontend_ru:
    deploy:
      replicas: 1

  frontend_en:
    deploy:
      replicas: 1

  app_ru:
    deploy:
      replicas: 1

  app_en:
    deploy:
      replicas: 1
